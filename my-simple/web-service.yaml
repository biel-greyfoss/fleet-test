apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web-app
    role: slave
    tier: backend
spec:
  ports:
  - port: 6379
  selector:
    app: web-app
    role: slave
    tier: backend
